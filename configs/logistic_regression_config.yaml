client: 5

round: 10

training:
  batch_size: 64
  learning_rate: 0.01

data:
  dataset_path: "hmeq.csv"
  smote: false
  rus: false
  scale: false
  encode: true
  pca: 0
  kbest: true
  batch_size: 64

aggregation:
  method: FedF1

server:
  address: "127.0.0.1:8080"

model:
  C: 0.1
  max_iter: 500
  penalty: l2
  class_weight: balanced
  solver: lbfgs
  n_jobs: -1
  warm_start: true
